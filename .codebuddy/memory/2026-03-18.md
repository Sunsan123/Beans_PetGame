## 2026-03-18

- 为用户生成了 ESP32-S3 + ST7789(8pin) + 3按钮 + 蜂鸣器的完整接线和烧录指南
- 项目 Beans_PetGame (JurassicLife) 使用 `DISPLAY_PROFILE 3` 配置对应 ESP32-S3 + ST7789 240x240
- 引脚定义在 `board_esp32s3_st7789.hpp` 中：屏幕 SPI (GPIO 9-14)、按钮 (GPIO 4/5/6)、蜂鸣器 (GPIO 21)
- 项目使用 LovyanGFX 作为屏幕驱动库（非 TFT_eSPI），使用 Arduino IDE 编译
- 用户提供了已跑通的 TFT_eSPI 屏幕接线配置，据此修改了 LovyanGFX 驱动引脚
- 屏幕引脚变更: SCLK=18, MOSI=17, MISO=6, DC=15, CS=16, RST=4, BL=14
- 按钮因 GPIO 4/6 被屏幕占用，改为: LEFT=7, RIGHT=5, OK=8
- 蜂鸣器 GPIO 21 和 SD 卡引脚不变

### JurassicLifeCN 中文化实施（续）
- 完成了所有文本渲染路径的 `setCNFont()`/`resetFont()` 调用添加（共19对）：
  - `touchBanner()` — tft 直接绘制中文校准提示
  - `drawActivityBar()` — 活动栏中文文本（吃东西/睡觉/便便等）
  - `petName` 绘制 — 中文恐龙名显示
  - `drawTopBtn` lambda — 顶部快捷按钮（吃/喝）
  - 底部按钮栏 `uiBot.drawString()` — 8个中文按钮标签
  - 小游戏-洗澡 标题、小游戏-玩耍 标题
  - 小游戏 HUD 底部（水滴/气球计数 + 退出提示）
  - 触摸校准倒计时屏幕、触摸屏已禁用消息、触摸校准失败消息
  - 两处"NO RAM"致命错误消息翻译为中文
- 删除了 CN 目录中多余的法语版 `DinoNames.h`（已由 `DinoNamesCN.h` 替代）
- Lint 检查通过，0 错误
- 所有用户可见的屏幕显示字符串均已翻译为中文
- 法语注释、变量名、JSON 键名保留不变（不影响用户体验，修改会破坏存档兼容性）
