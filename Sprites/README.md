# Legacy Sprite Compatibility

`Sprites/` is no longer the authoritative source tree for game art.

Use [assets](C:/Users/17787/Desktop/Beans_PetGame/assets) for all new or edited asset source files and manifests.

This folder remains only for compatibility:

- `build_sprites.py`: wrapper entrypoint for the new asset pipeline
- `build_sprites.exe`: existing packaged builder entrypoint

Do not add new source art under `Sprites/`.
